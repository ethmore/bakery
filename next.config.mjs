/** @type {import('next').NextConfig} */
const nextConfig = {
    basePath: 'https://bakery.webeveyn.com',
    output: 'export',
    images: {
        unoptimized: true
    }
};

export default nextConfig;
